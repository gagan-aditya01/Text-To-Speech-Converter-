[pytest]
addopts = -v --tb=short
markers =
    integration: end-to-end tests that make real network calls (deselect with -m "not integration")
    slow: tests that take more than 2 seconds (deselect with -m "not slow")
